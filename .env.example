DJANGO_SECRET_KEY=change-me
DEBUG=True
IP_HASH_SALT=change-me-too
POSTGRES_PASSWORD=change-me-too
ALLOWED_HOSTS=yourdomain.com
DATABASE_URL=   # leave blank in dev (SQLite); set postgres://... in prod
#SEED_DEMO=1    # enable demo seeding (dev only; disabled by default)

# Email for exclusive-post unlock emails (console backend prints to stdout in dev)
#EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
#DEFAULT_FROM_EMAIL=NamoLead <no-reply@namolead.in>
#EMAIL_HOST=smtp-relay.brevo.com
#EMAIL_PORT=587
#EMAIL_HOST_USER=
#EMAIL_HOST_PASSWORD=
#UNLOCK_RATE_LIMIT=5      # unlock requests per IP per hour
#UNLOCK_LINK_TTL_MINUTES=15
